/*
 * Hello world with args.
 */
procedure main(argc: integer; argv: string_vector_type) is
begin
	int num=.4;
	char letter='a';
    printf("Hello, world\n");//banana
    loop
        argc := argc - 1;
        exit when (argc = 0);
        printf("arg[%d]: %s\n", argc, argv[argc]);
    end loop;
end main;